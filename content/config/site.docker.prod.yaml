environment: "production"
base_url: "https://example.com"

server:
  addr: ":8080"
  live_reload: false
  auto_open_browser: false
  debug_routes: false

admin:
  local_only: false
  debug:
    pprof: false

backup:
  dir: "data/backups"
