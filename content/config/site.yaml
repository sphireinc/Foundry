title: "Foundry CMS"
base_url: "http://localhost:8080"
theme: "default"
default_lang: "en"
content_dir: "content"
public_dir: "public"

server:
  addr: ":8080"

permalinks:
  page: "/:lang/:slug/"
  post: "/:lang/posts/:slug/"